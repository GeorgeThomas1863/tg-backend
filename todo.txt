TODO — get the app running (single-video scope)

Current state (as of last review):
- Backend code: complete, no issues found.
- Backend env: NOT initialized — no .venv (uv sync never run), no Telethon session file (login never done).
- .env exists at repo root but TELEGRAM_API_ID / TELEGRAM_API_HASH / TELEGRAM_CHANNEL are not filled in yet.
- Frontend code: complete, already scaffolded (package.json, vite.config.js, index.html, src/*).
- Frontend env: node_modules already installed, ready to run.
- Gallery/grid (multi-video) is explicitly OUT of scope for this pass — App.jsx intentionally stays single-video until this list is done.

Steps:

1. [YOU] Get Telegram API credentials at https://my.telegram.org -> API development tools -> copy api_id and api_hash.

2. [YOU] Pick the Telegram channel/chat that holds the videos; note its @username or numeric ID. It needs at least one video message the account can see.

3. [YOU] Fill in root .env:
   TELEGRAM_API_ID=...
   TELEGRAM_API_HASH=...
   TELEGRAM_CHANNEL=...
   (FRONTEND_ORIGIN is optional, defaults to http://localhost:5173)

4. [ME] Run `uv sync` in backend/ to create the venv and install deps.

5. [YOU, interactive] Start `uv run uvicorn main:app --reload --env-file ../.env` from backend/. Telethon's first-run login prompt (phone number, then the code Telegram sends) appears in that terminal — only you can answer it. Succeeds once -> saves backend/session, never asks again.

6. [ME] Verify backend alone: GET http://localhost:8000/api/videos should return JSON for the channel's videos. Empty list means wrong channel/ID or no video messages.

7. [ME] Run `npm run dev` in frontend/ -> serves at http://localhost:5173.

8. [ME] Browser check: load the page, confirm thumbnail/metadata render, play the video, and specifically seek around (forward and backward) — this exercises the Range-request -> 4096-alignment path in telegram.py, the trickiest part of the system.

9. [ME] Fix whatever step 8 surfaces — likely candidates: CORS origin mismatch, mime-type edge cases, or an off-by-one in the range math that only shows up on seek.

Not in this pass:
- Gallery/grid rework of App.jsx (render multiple videos instead of just videos[0]) — separate follow-up once single-video is confirmed solid.
